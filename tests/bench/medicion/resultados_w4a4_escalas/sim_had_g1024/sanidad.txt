7.1 s | El sistema de archivos de Linux organiza los datos en inodos y bloques.
24.3 s | 294 tok | 12.1 tok/s
```python
def primeros_primos(n: int) -> list[int]:
    """
    Devuelve una lista con los primeros n números primos.

    Parámetros:
        n (int): Cantidad de números primos a devolver. Debe ser un entero positivo.

    Retorna:
        list[int]: Lista con los primeros n números primos.

    Ejemplo:
        >>> primeros_primos(5)
        [2, 3, 5, 7, 11]
    """
    if n <= 0:
        return []
    
    primos = []
    candidato = 2
    
    while len(primos) < n:
        es_primo = True
        for p in primos:
            if p * p > candidato:
                break
            if candidato % p == 0:
                es_primo = False
                break
        if es_primo:
        
