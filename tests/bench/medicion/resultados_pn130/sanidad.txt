3.7 s | El sistema de archivos de Linux organiza los datos en inodos y bloques.
2.1 s | 311 tok | 148.2 tok/s
```python
def primeros_n_primos(n: int) -> list[int]:
    """Devuelve una lista con los primeros n números primos.

    Args:
        n: Cantidad de números primos a devolver. Debe ser un entero positivo.

    Returns:
        Una lista con los primeros n números primos en orden ascendente.

    Raises:
        ValueError: Si n es menor o igual a 0.

    Example:
        >>> primeros_n_primos(5)
        [2, 3, 5, 7, 11]
    """
    if n <= 0:
        raise ValueError("n debe ser un entero positivo")

    primos = []
    candidato = 2

    while len(primos) < n:
        es_primo = True
        for p in primos:
            if p * p > candidato:
                break
            if candidato % 
