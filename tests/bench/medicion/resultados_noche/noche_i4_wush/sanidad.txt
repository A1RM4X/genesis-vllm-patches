3.9 s | El sistema de archivos de Linux organiza los datos en inodos y bloques.
2.3 s | 331 tok | 141.6 tok/s
```python
def primeros_n_primos(n: int) -> list[int]:
    """Devuelve una lista con los primeros n números primos.

    Args:
        n: Cantidad de números primos a generar. Debe ser un entero positivo.

    Returns:
        Una lista de enteros conteniendo los primeros n números primos.

    Raises:
        ValueError: Si n es menor o igual a 0.

    Example:
        >>> primeros_n_primos(5)
        [2, 3, 5, 7, 11]
    """
    if n <= 0:
        raise ValueError("n debe ser un entero positivo")

    primos = []
    candidato = 2

    while len(primos) < n:
        # Verificar si 'candidato' es primo
        es_primo = True
        for p in primos:
            if p * p > candidato:
       
