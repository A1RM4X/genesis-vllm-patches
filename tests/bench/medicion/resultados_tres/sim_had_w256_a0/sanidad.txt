7.2 s | El sistema de archivos de Linux organiza los datos en inodos y bloques.
25.9 s | 313 tok | 12.1 tok/s
```python
def primeros_n_primos(n: int) -> list[int]:
    """
    Devuelve una lista con los primeros n números primos.

    Parámetros:
        n (int): Cantidad de números primos a devolver. Debe ser un entero positivo.

    Retorna:
        list[int]: Lista con los primeros n números primos.

    Ejemplo:
        >>> primeros_n_primos(5)
        [2, 3, 5, 7, 11]
    """
    if n <= 0:
        raise ValueError("n debe ser un entero positivo")
    
    primos = []
    candidato = 2
    
    while len(primos) < n:
        # Verificar si candidato es primo
        es_primo = True
        for p in primos:
            if p * p > candidato:
                break
            if candidato % p == 0
